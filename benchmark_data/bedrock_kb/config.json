{
  "description": "Arm configurations for the Bedrock KB anchor benchmark. Committed before any run so the configs are on record. Bedrock is an anchor, not a subject; no sweep.",
  "region": "us-east-1",
  "embedding_model": "amazon.titan-embed-text-v2:0",
  "vector_store": "s3_vectors",
  "budget_tokens": 2000,
  "token_chars": 4,
  "arms": {
    "P": {
      "tool": "pdf_corpus_search",
      "mode": "auto",
      "excerpt_style": "paragraph",
      "label": "P-para"
    },
    "P-snippet": {
      "label": "P-snip",
      "tool": "pdf_corpus_search",
      "mode": "auto",
      "excerpt_style": "snippet",
      "note": "Same retrieval as P, different excerpt style. Included because excerpt style proved a larger lever than any Bedrock config difference measured here."
    },
    "B0-default-v1": {
      "label": "B0",
      "parser": "default",
      "chunking": {
        "strategy": "NONE_OVERRIDE_DEFAULT",
        "note": "Bedrock default: ~300 tokens, sentence-aware"
      },
      "rerank": null
    },
    "B1-fixed1000-v1": {
      "label": "B1",
      "parser": "default",
      "chunking": {
        "strategy": "FIXED_SIZE",
        "maxTokens": 1000,
        "overlapPercentage": 20
      },
      "rerank": "cohere.rerank-v3-5:0"
    },
    "P-window": {
      "label": "P-win",
      "tool": "pdf_corpus_search",
      "mode": "auto",
      "excerpt_style": "window",
      "note": "Same retrieval as P; excerpt is a ~600-token contiguous block window around the anchor (keyword hit, best sub-page chunk, or page top). Fewer, larger units at the same token budget."
    },
    "P-window300": {
      "label": "P-win300",
      "tool": "pdf_corpus_search",
      "mode": "auto",
      "excerpt_style": "window",
      "window_tokens": 300,
      "note": "Window arm at half the default budget per excerpt (~6 hits fit 2,000 tokens instead of ~3), to separate the unit-shape effect from the coverage cost on spread."
    },
    "P-auto": {
      "label": "P-auto",
      "tool": "pdf_corpus_search",
      "mode": "auto",
      "excerpt_style": "auto",
      "note": "Per-query excerpt shape chosen from the number of documents with an AND keyword match (0 paragraph, 1 window, 2+ snippet). Same retrieval as P; only the returned unit differs. Candidate for a single configuration that concedes no class."
    },
    "P-eb600": {
      "label": "P-eb600",
      "tool": "pdf_corpus_search",
      "mode": "auto",
      "evidence_budget": 600,
      "note": "The shipped product form of P-auto: pdf_corpus_search(evidence_budget=600) routes the unit server-side. The harness asserts it is identical to P-auto query for query; it exists so a server-side drift in the rule shows up here."
    }
  },
  "revision_rule": "An arm id names one immutable index. To change a config, add a new id with a bumped -vN suffix and provision it; never edit an existing id in place. `label` is the short name used in result tables.",
  "excluded": {
    "HIERARCHICAL": "AWS does not recommend with S3 vector buckets (metadata size limits)",
    "SEMANTIC": "rejects files over 1 MB body text (Arroyo 2026); several corpus docs exceed it",
    "NONE": "Titan v2 caps 8,192 tokens per request; most corpus docs exceed it",
    "bedrock_data_automation": "$0.010/page, ~$22 per corpus; not needed for an anchor"
  }
}
